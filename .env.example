# Override the deployed base path (defaults to /un-data-commons-field-guide/).
# BASE_PATH=/

# The UN System Data Commons API root. Documented here so the value is never
# hard-coded in a component; see src/lib/undc/config.ts.
VITE_UNDC_API_ROOT=https://unsd-datacommons.gcp.un-icc.cloud
